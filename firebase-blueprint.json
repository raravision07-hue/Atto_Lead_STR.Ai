{
  "entities": {
    "Lead": {
      "title": "Lead",
      "description": "A business lead found or added by the user.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "businessName": { "type": "string" },
        "websiteUrl": { "type": "string" },
        "industry": { "type": "string" },
        "email": { "type": "string" },
        "phone": { "type": "string" },
        "socialMedia": {
          "type": "object",
          "properties": {
            "facebook": { "type": "string" },
            "linkedin": { "type": "string" },
            "instagram": { "type": "string" }
          }
        },
        "websiteIssues": {
          "type": "array",
          "items": { "type": "string" }
        },
        "opportunityScore": { "type": "number" },
        "cms": { "type": "string" },
        "details": { "type": "string" },
        "userId": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["businessName", "userId", "createdAt"]
    },
    "UserProfile": {
      "title": "User Profile",
      "description": "Private user information.",
      "type": "object",
      "properties": {
        "displayName": { "type": "string" },
        "email": { "type": "string" },
        "photoURL": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["email", "createdAt"]
    }
  },
  "firestore": {
    "users/{userId}/leads/{leadId}": {
      "schema": "Lead",
      "description": "Collection of leads owned by a specific user."
    },
    "users/{userId}/private/profile": {
      "schema": "UserProfile",
      "description": "Private profile data for a user."
    }
  }
}
